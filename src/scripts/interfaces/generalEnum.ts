enum General {
  words = '/words',
  users = '/users',
  signin = '/signin',
}

enum SubGeneral {
  tokens = '/tokens',
  aggregatedWords = '/aggregatedWords',
  statistics = '/statistics',
  settings = '/settings',
}

export { General, SubGeneral };
